dataContractSpecification: 1.1.0
id: ''
info:
  title: BDH Data Contract for datahub_dev_silver-consumer-dim_products
  version: 1.0.0
  description: Product dimension table (cleansed)
  owner: BDH
  status: active
  contact:
    name: John Doe
    email: abc@gmail.com

models:
  datahub_dev_silver-consumer-dim_products:
    description: Product dimension table (cleansed)
    type: table
    fields:
      product_id:
        description: product id
        type: int
        required: true
        unique: true
      product_name:
        description: product name
        type: string
        required: true
      category:
        description: product category
        type: string
        required: true
        enum: ["Electronics", "Grocery", "Sports", "Furniture", "Clothing", "Toys"]
      price:
        description: product price
        type: decimal
        required: true
      supplier_id:
        description: supplier foreign key
        type: string
        required: true
      extract_timestamp:
        description: source extract timestamp
        type: string
        required: true
      dbx_extract_timestamp:
        description: dbx extract timestamp
        type: timestamp
        required: true

    primaryKey:
      - product_id
